# Core requirements for Gleitzeit V5
python-socketio>=5.8.0
aiohttp>=3.8.0
pydantic>=2.0.0
pyyaml>=6.0.0
httpx>=0.24.0

# CLI and UX
rich>=13.0.0
click>=8.0.0

# Optional but recommended
asyncio-mqtt>=0.13.0

# Development dependencies (install with pip install -e .[dev])
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.0.0
# ruff>=0.1.0