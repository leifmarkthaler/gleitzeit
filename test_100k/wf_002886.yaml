name: 'Performance Test 2886'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 2886,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
