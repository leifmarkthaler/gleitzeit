name: 'Performance Test 74187'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 74187,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
