name: 'Performance Test 3039'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 3039,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
