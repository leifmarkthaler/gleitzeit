name: 'Performance Test 20671'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 20671,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
