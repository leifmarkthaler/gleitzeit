name: 'Performance Test 52551'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 52551,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
