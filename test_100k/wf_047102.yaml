name: 'Performance Test 47102'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 47102,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
