name: 'Performance Test 43723'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 43723,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
