name: 'Performance Test 69894'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 69894,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
