name: 'Performance Test 91487'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 91487,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
