name: 'Performance Test 32783'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 32783,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
