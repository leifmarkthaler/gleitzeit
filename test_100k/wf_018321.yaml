name: 'Performance Test 18321'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 18321,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
