name: 'Performance Test 56474'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 56474,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
