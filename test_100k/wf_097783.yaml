name: 'Performance Test 97783'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 97783,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
