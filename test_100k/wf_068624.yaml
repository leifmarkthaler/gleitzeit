name: 'Performance Test 68624'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 68624,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
