name: 'Performance Test 54851'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 54851,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
