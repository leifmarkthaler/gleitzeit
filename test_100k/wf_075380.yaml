name: 'Performance Test 75380'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 75380,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
