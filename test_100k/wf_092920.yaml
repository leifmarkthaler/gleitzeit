name: 'Performance Test 92920'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 92920,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
