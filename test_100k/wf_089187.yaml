name: 'Performance Test 89187'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 89187,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
