name: 'Performance Test 34259'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 34259,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
