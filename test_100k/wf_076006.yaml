name: 'Performance Test 76006'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 76006,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
