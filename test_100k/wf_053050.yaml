name: 'Performance Test 53050'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 53050,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
