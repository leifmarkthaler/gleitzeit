name: 'Performance Test 9108'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 9108,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
