name: 'Performance Test 71910'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 71910,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
