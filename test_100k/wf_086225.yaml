name: 'Performance Test 86225'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 86225,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
