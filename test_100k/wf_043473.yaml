name: 'Performance Test 43473'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 43473,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
