name: 'Performance Test 7353'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 7353,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
