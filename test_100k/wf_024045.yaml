name: 'Performance Test 24045'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 24045,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
