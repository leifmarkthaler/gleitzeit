name: 'Performance Test 98978'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 98978,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
