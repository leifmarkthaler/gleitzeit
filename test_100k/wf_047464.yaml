name: 'Performance Test 47464'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 47464,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
