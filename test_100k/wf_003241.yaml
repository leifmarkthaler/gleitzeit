name: 'Performance Test 3241'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 3241,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
