name: 'Performance Test 63080'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 63080,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
