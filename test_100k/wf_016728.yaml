name: 'Performance Test 16728'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 16728,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
