name: 'Performance Test 9939'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 9939,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
