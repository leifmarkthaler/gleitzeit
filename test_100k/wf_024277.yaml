name: 'Performance Test 24277'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 24277,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
