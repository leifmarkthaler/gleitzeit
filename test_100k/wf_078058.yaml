name: 'Performance Test 78058'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 78058,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
