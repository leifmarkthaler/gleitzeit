name: 'Performance Test 16290'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 16290,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
