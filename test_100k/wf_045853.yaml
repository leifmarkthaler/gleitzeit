name: 'Performance Test 45853'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 45853,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
