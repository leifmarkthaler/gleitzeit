name: 'Performance Test 78942'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 78942,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
