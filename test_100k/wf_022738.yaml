name: 'Performance Test 22738'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 22738,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
