name: 'Performance Test 18814'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 18814,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
