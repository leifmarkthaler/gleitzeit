name: 'Performance Test 71761'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 71761,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
