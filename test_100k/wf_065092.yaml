name: 'Performance Test 65092'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 65092,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
