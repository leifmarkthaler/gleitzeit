name: 'Performance Test 48498'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 48498,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
