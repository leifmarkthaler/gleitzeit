name: 'Performance Test 17088'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 17088,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
