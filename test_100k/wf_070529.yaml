name: 'Performance Test 70529'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 70529,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
