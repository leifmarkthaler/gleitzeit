name: 'Performance Test 31901'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 31901,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
