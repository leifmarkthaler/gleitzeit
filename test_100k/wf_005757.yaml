name: 'Performance Test 5757'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 5757,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
