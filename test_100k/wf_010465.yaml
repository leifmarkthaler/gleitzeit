name: 'Performance Test 10465'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 10465,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
