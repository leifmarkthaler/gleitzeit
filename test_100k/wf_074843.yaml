name: 'Performance Test 74843'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 74843,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
