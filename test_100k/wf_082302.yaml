name: 'Performance Test 82302'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 82302,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
