name: 'Performance Test 6622'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 6622,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
