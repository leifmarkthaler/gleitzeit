name: 'Performance Test 41005'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 41005,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
