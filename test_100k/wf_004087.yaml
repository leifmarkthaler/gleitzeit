name: 'Performance Test 4087'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 4087,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
