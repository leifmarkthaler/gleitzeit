name: 'Performance Test 27030'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 27030,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
