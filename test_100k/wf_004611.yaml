name: 'Performance Test 4611'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 4611,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
