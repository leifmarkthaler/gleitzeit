name: 'Performance Test 11459'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 11459,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
