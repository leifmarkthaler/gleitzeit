name: 'Performance Test 41827'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 41827,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
