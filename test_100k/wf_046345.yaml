name: 'Performance Test 46345'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 46345,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
