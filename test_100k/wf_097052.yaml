name: 'Performance Test 97052'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 97052,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
