name: 'Performance Test 80139'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 80139,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
