name: 'Performance Test 20113'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 20113,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
