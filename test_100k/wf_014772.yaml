name: 'Performance Test 14772'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 14772,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
