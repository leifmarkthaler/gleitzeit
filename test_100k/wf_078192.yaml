name: 'Performance Test 78192'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 78192,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
