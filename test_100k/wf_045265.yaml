name: 'Performance Test 45265'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 45265,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
