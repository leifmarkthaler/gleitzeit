name: 'Performance Test 53879'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 53879,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
