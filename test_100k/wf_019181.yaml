name: 'Performance Test 19181'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 19181,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
