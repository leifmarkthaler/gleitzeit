name: 'Performance Test 55395'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 55395,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
