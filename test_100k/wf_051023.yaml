name: 'Performance Test 51023'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 51023,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
