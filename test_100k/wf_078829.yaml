name: 'Performance Test 78829'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 78829,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
