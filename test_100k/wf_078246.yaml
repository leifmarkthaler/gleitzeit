name: 'Performance Test 78246'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 78246,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
