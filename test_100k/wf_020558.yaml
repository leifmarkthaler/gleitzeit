name: 'Performance Test 20558'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 20558,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
