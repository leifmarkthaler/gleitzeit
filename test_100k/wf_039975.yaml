name: 'Performance Test 39975'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 39975,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
