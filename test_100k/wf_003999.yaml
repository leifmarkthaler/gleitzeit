name: 'Performance Test 3999'
version: '1.0.0'
tasks:
  - id: 'task_1'
    type: 'python'
    method: 'python/execute'
    params:
      code: |
        import time
        start = time.time()
        result = {
            'workflow_id': 3999,
            'calculation': sum(range(100)),
            'duration': time.time() - start
        }
