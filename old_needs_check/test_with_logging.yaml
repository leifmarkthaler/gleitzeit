# Test configuration with file logging enabled
logging:
  file_logging_enabled: true
  log_dir: "logs"

redis:
  url: "redis://localhost:6379"
  
sharding:
  enabled: true
  num_shards: 16

workers:
  api:
    enabled: true
  workflow_loader:
    enabled: true
