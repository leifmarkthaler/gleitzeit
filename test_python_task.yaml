name: "Test Python Task"
description: "A simple test task using Python execution"
version: "1.0"

timeout: 30
wait_for_completion: true

tasks:
  - id: "hello_world"
    method: "python/execute"
    priority: 2
    parameters:
      code: |
        print("Hello from Gleitzeit!")
        result = 2 + 2
        print(f"2 + 2 = {result}")
        return {"message": "Task completed successfully", "result": result}